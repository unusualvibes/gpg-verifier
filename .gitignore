# Node modules (for testing only)
node_modules/

# Test files - large binary test files only
# (keep test directory and its structure, only exclude large files)
test/test-data/test-large-*.bin
test/test-data/test-large-*.iso
test/test-data/*.iso

# ISO files (too large for GitHub)
*.iso

# Generated packages (keep the versioned release for local downloads)
# gpg-verifier-offline-v*.zip is served by the web app
# Ignore test packages only:
gpg-verifier-offline-test*.zip

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
